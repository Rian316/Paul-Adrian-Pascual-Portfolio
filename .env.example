# Public production URL — powers canonical links, sitemap.xml, robots.txt,
# and Open Graph / Twitter image URLs. No trailing slash.
# Set this in Vercel → Project → Settings → Environment Variables.
NEXT_PUBLIC_SITE_URL=https://paulpascual.dev
